
/**
 * This is the core of the challenge.
 */
export const compareJSON = (firstJson, secondJson) => {
    const fKeys = Object.keys(firstJson);
    const sKeys = Object.keys(secondJson);

    console.log("fKeys", fKeys);
    console.log("sKeys", sKeys);
    
    return 0.1;
}